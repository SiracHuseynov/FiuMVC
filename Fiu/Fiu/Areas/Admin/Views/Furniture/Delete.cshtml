@using Fiu.Core.Models
@model Furniture

<div class="container">
    <div class="row">
        <div class="col-6">
            <form method="post" asp-action="DeletePost">
                <div class="card" style="width: 18rem;">
                    <img src="~/uploads/furnitures/@Model.ImageUrl" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">@Model.Title</h5>
                        <p class="card-text">@Model.Price</p>
                        <p class="card-text">@Model.RedirectUrl</p>
                        <input asp-for="@Model.Id" hidden value="@Model.Id" /> 
                        <a asp-action="Index" class="btn btn-info">Go back</a>
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>